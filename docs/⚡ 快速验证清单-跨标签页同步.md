# ⚡ 快速验证清单 - 跨标签页认证同步修复

## 🎯 30 秒快速测试

### ✅ 测试 1：跨标签页同步（最核心）

1. 打开 `http://localhost:3000`
2. 点击 "Sign In" → 使用 Google/GitHub 登录
3. **看到用户头像后**，打开**新标签页**
4. 在新标签页访问 `http://localhost:3000`

**✅ 预期结果**：
- 新标签页**立即显示用户头像**（右上角）
- **无 "Sign In" 按钮闪烁**

**❌ 如果失败**：
- 看到 "Sign In" 按钮 → 1-2秒后变为头像（说明修复未生效）

---

### ✅ 测试 2：登出清理

1. 登录后，点击用户头像 → "Sign out"
2. 打开 **Chrome DevTools** → `Application` → `Local Storage` → `http://localhost:3000`
3. 查看存储项

**✅ 预期结果**：
| 存储项 | 状态 |
|--------|------|
| `lumi_auth_cache` | ❌ 不存在 |
| `lumi_user_tier` | ❌ 不存在 |
| `lumi_usage_data_v2` | ❌ 不存在 |
| `lumi-cookie-consent` | ✅ 仍存在（保留） |

**❌ 如果失败**：
- 看到 `lumi_auth_cache` 仍存在 → 清理逻辑未执行

---

### ✅ 测试 3：控制台日志检查

1. 打开 **Chrome DevTools** → `Console`
2. 登录账号
3. 打开新标签页

**✅ 预期日志**：
```
[Auth] 📦 Using cached user (valid for XXX more seconds)
```

**❌ 如果看到**：
```
[Auth] Fetching user from API...
```
→ 说明没有使用缓存，修复未生效

---

## 🔍 详细验证（如果时间充足）

### 测试 4：缓存过期验证

1. 登录账号
2. **关闭所有浏览器标签页**
3. **等待 6 分钟**（或手动修改 `CACHE_DURATION` 为 10 秒测试）
4. 重新打开浏览器，访问网站

**✅ 预期日志**：
```
[Auth] 🗑️ Cache expired, clearing
```

---

### 测试 5：多标签页登出同步

1. 打开标签页 A 和 B（都登录状态）
2. 在标签页 A 点击登出
3. **立即切换到标签页 B**，刷新页面

**✅ 预期**：标签页 B 显示未登录状态

---

## 🐛 常见问题排查

### 问题 1：新标签页仍然闪烁

**可能原因**：
1. 浏览器缓存未清除
2. 代码未正确部署

**解决方法**：
```bash
# 1. 清除浏览器所有数据
Chrome → Settings → Privacy → Clear browsing data → All time

# 2. 重启开发服务器
npm run dev
```

---

### 问题 2：localStorage 数据未清除

**检查代码**：
```typescript
// hooks/use-auth.ts 是否有这个函数？
const clearAllUserData = () => {
  localStorage.removeItem(AUTH_CACHE_KEY)
  localStorage.removeItem(TIER_STORAGE_KEY)
  localStorage.removeItem(USAGE_STORAGE_KEY)
}
```

**检查调用**：
```typescript
// signOut 函数是否调用了 clearAllUserData()？
const signOut = async () => {
  await fetch("/api/auth/logout", { method: "POST" })
  setUser(null)
  clearAllUserData()  // ← 这里必须调用
}
```

---

### 问题 3：控制台报错

**常见错误**：
```
localStorage is not defined
```

**原因**：服务端渲染时访问 localStorage

**检查代码**：
```typescript
// 必须有这个检查
if (typeof window !== "undefined") {
  localStorage.getItem(...)
}
```

---

## 📊 性能验证

### 打开 Network 标签

1. 清除浏览器缓存
2. 登录账号（标签页 A）
3. 打开新标签页（标签页 B）
4. 查看 Network 标签

**✅ 预期**：
- 标签页 A：调用 `/api/user-info` ✅
- 标签页 B：**不调用** `/api/user-info` ✅（使用缓存）

**❌ 如果失败**：
- 标签页 B 也调用了 `/api/user-info` → 缓存未命中

---

## ✅ 验证通过标准

- [x] 新标签页立即显示用户头像（无闪烁）
- [x] 控制台显示 `📦 Using cached user`
- [x] 登出后 localStorage 完全清空（除了 cookie-consent）
- [x] 新标签页不调用 `/api/user-info`（Network 标签验证）

**全部通过** → 🎉 修复成功！

---

## 🚀 上线前最后检查

### 代码检查

```bash
# 运行 Linter
npm run lint

# 检查类型错误
npm run type-check

# 构建生产版本
npm run build
```

**✅ 预期**：无错误输出

---

### 文件检查清单

- [x] `hooks/use-auth.ts` - sessionStorage → localStorage
- [x] `hooks/use-auth.ts` - 新增 `clearAllUserData()`
- [x] `contexts/usage-limit-context.tsx` - 移除重复清理逻辑
- [x] 测试指南已创建
- [x] 完成文档已创建

---

## 📞 遇到问题？

### 调试技巧

1. **查看完整日志**：
   ```typescript
   // 搜索所有 [Auth] 日志
   console.log 过滤：[Auth]
   ```

2. **检查 localStorage**：
   ```javascript
   // 在 Console 中运行
   console.log('Auth cache:', localStorage.getItem('lumi_auth_cache'))
   console.log('User tier:', localStorage.getItem('lumi_user_tier'))
   console.log('Usage data:', localStorage.getItem('lumi_usage_data_v2'))
   ```

3. **手动清除缓存**：
   ```javascript
   // 在 Console 中运行
   localStorage.clear()
   ```

---

**测试人**：___________  
**测试时间**：___________  
**测试结果**：✅ 通过 / ❌ 失败

**备注**：
___________________________________________
___________________________________________

