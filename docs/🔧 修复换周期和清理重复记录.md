# 🔧 修复换周期判断和清理重复记录

## 🔍 问题总结

### 问题 1：换周期被误判为续约 ✅ 已修复

**现象**：Basic Monthly → Basic Yearly 被判断为"续约"

**原因**：旧代码中的判断逻辑问题

**状态**：✅ 代码已修复（最新代码是正确的）

---

### 问题 2：历史记录中有重复的 "Upgraded" ⚠️ 数据清理

**现象**：Upgraded 显示了 2 次

**原因**：在修复幂等性检查之前产生的重复记录

**解决**：清理数据库中的重复记录

---

## 📊 验证当前代码

### 检查逻辑是否正确

当前代码（已修复）：

```typescript
// 判断订阅类型
if (!existingSubscription) {
  eventType = "subscription_created"
} else {
  const oldTier = existingSubscription.tier
  const oldCycle = existingSubscription.billing_cycle
  const newTier = tierInfo.tier
  const newCycle = tierInfo.billingCycle

  // 判断是否为相同套餐
  isSamePlan = (oldTier === newTier && oldCycle === newCycle)

  if (isSamePlan) {
    // ✅ 完全相同：Basic Monthly → Basic Monthly
    eventType = "subscription_renewed"
  } else if (oldTier === newTier && oldCycle !== newCycle) {
    // ✅ 换周期：Basic Monthly → Basic Yearly
    eventType = "subscription_cycle_changed"
  } else {
    // ✅ 升级/降级：Basic → Pro
    if (tierLevel[newTier] > tierLevel[oldTier]) {
      eventType = "subscription_upgraded"
    } else {
      eventType = "subscription_downgraded"
    }
  }
}
```

**逻辑正确** ✅

---

## 🧪 测试验证

### 测试场景 1：换周期

**操作**：Basic Monthly → Basic Yearly

**预期日志**：
```bash
🔄 [Webhook] Event type: Billing cycle changed (monthly → yearly)
✅ [Webhook] Starting new yearly period
```

**预期历史记录**：
```
event_type: subscription_cycle_changed  ✅
description: Changed basic from monthly to yearly
```

---

### 测试场景 2：升级

**操作**：Basic Yearly → Pro Yearly

**预期日志**：
```bash
⬆️ [Webhook] Event type: Upgraded (basic → pro)
✅ [Webhook] Starting new yearly period
```

**预期历史记录**：
```
event_type: subscription_upgraded  ✅
description: Upgraded from basic to pro
```

---

## 🔧 清理重复记录

### 步骤 1：查询重复记录

在 Supabase SQL Editor 中执行：

```sql
-- 查看你的所有历史记录
SELECT 
  id,
  event_type,
  description,
  amount,
  creem_payment_id,
  DATE(event_date) as date,
  event_date
FROM subscription_history
WHERE user_id = '88dd0f65-b513-48c6-8c9a-e217147a2b6f'
ORDER BY event_date DESC;
```

**查找重复的 "Upgraded" 记录**：
- 看是否有相同的 `creem_payment_id`
- 相同 payment_id = 重复记录

---

### 步骤 2：删除重复记录

**方法 A：删除特定 ID**

如果知道重复记录的 ID：

```sql
-- 删除重复的记录（替换为实际的 ID）
DELETE FROM subscription_history 
WHERE id = 'duplicate-record-id-here';
```

---

**方法 B：自动清理重复**

```sql
-- 查找并删除重复的 payment_id 记录（保留最早的）
WITH duplicates AS (
  SELECT 
    id,
    creem_payment_id,
    ROW_NUMBER() OVER (
      PARTITION BY user_id, creem_payment_id 
      ORDER BY event_date ASC  -- 保留最早的
    ) as rn
  FROM subscription_history
  WHERE user_id = '88dd0f65-b513-48c6-8c9a-e217147a2b6f'
    AND creem_payment_id IS NOT NULL
)
DELETE FROM subscription_history
WHERE id IN (
  SELECT id FROM duplicates WHERE rn > 1
);

-- 返回删除的记录数
```

---

### 步骤 3：验证清理

```sql
-- 再次查询，确认没有重复
SELECT 
  event_type,
  description,
  amount,
  creem_payment_id,
  COUNT(*) as count
FROM subscription_history
WHERE user_id = '88dd0f65-b513-48c6-8c9a-e217147a2b6f'
GROUP BY event_type, description, amount, creem_payment_id
HAVING COUNT(*) > 1;

-- 如果没有结果 = 没有重复 ✅
```

---

## 📊 验证正确性

### 正确的历史记录应该是

根据你的购买流程：

```
1. subscription_created
   - Basic Monthly ($4.99)
   
2. subscription_cycle_changed  ← 应该是这个（不是 renewed）
   - Basic Monthly → Yearly ($49.00)
   
3. subscription_upgraded
   - Basic Yearly → Pro Yearly ($99.00)

Total: 3 条记录
Total Spent: $4.99 + $49.00 + $99.00 = $152.99 ✅
```

---

## ✅ 修复方案

### 修复 1：清理旧的错误记录

**如果 Basic Monthly → Yearly 被记录为 `subscription_renewed`**：

```sql
-- 查找并修正错误的事件类型
SELECT 
  id,
  event_type,
  description,
  tier,
  billing_cycle,
  metadata
FROM subscription_history
WHERE user_id = '88dd0f65-b513-48c6-8c9a-e217147a2b6f'
  AND event_type = 'subscription_renewed'
  AND tier = 'basic'
  AND metadata LIKE '%old_billing_cycle%'  -- 如果有换周期信息
ORDER BY event_date DESC;

-- 如果确认是换周期，手动修正：
UPDATE subscription_history
SET 
  event_type = 'subscription_cycle_changed',
  description = 'Changed basic from monthly to yearly'
WHERE id = 'record-id-to-fix';
```

---

### 修复 2：删除重复的 Upgraded 记录

```sql
-- 查找重复的 upgraded 记录
SELECT 
  id,
  creem_payment_id,
  event_date,
  description
FROM subscription_history
WHERE user_id = '88dd0f65-b513-48c6-8c9a-e217147a2b6f'
  AND event_type = 'subscription_upgraded'
ORDER BY event_date;

-- 如果有 2 条相同 payment_id 的记录，删除后面的：
DELETE FROM subscription_history
WHERE id = 'later-duplicate-record-id';
```

---

## 🚀 完整的清理和验证流程

### 一键清理脚本

在 Supabase SQL Editor 中执行：

```sql
-- ===================================
-- 1. 查看当前所有记录
-- ===================================
SELECT 
  event_type,
  tier,
  billing_cycle,
  amount,
  description,
  DATE(event_date) as date
FROM subscription_history
WHERE user_id = '88dd0f65-b513-48c6-8c9a-e217147a2b6f'
ORDER BY event_date DESC;

-- ===================================
-- 2. 删除重复记录（基于 payment_id）
-- ===================================
WITH duplicates AS (
  SELECT 
    id,
    creem_payment_id,
    event_date,
    ROW_NUMBER() OVER (
      PARTITION BY user_id, creem_payment_id 
      ORDER BY event_date ASC
    ) as rn
  FROM subscription_history
  WHERE user_id = '88dd0f65-b513-48c6-8c9a-e217147a2b6f'
    AND creem_payment_id IS NOT NULL
)
DELETE FROM subscription_history
WHERE id IN (
  SELECT id FROM duplicates WHERE rn > 1
);

-- ===================================
-- 3. 验证清理结果
-- ===================================
SELECT 
  COUNT(*) as total_records,
  SUM(amount) as total_spent
FROM subscription_history
WHERE user_id = '88dd0f65-b513-48c6-8c9a-e217147a2b6f'
  AND status = 'completed';

-- 预期：
-- total_records: 3 条（不是 4 条）
-- total_spent: $152.99
```

---

## 📋 完整验证清单

### 数据库验证

- [ ] 执行清理 SQL
- [ ] 确认只有 3 条记录（不是 4 条）
- [ ] 确认 Total Spent = $152.99

### Dashboard 验证

- [ ] 刷新 Dashboard 页面
- [ ] Total Spent 显示 $152.99
- [ ] Renewals 显示正确数量
- [ ] "Upgraded" 只显示 1 次

### 新购买测试

- [ ] 测试换周期（Monthly → Yearly）
- [ ] 日志显示："Billing cycle changed"
- [ ] 历史记录：`subscription_cycle_changed`

---

## 🎯 总结

**问题 1**：换周期被误判为续约
- **原因**：旧代码的 bug
- **状态**：✅ 代码已修复
- **建议**：重启服务器，使用新代码

**问题 2**：重复的 Upgraded 记录
- **原因**：修复幂等性之前产生的重复数据
- **状态**：✅ 新代码已防止重复
- **建议**：执行清理 SQL 删除旧的重复记录

**下一步**：
1. 在 Supabase 执行清理 SQL
2. 刷新 Dashboard 验证
3. 测试新的购买流程

---

**需要我帮你生成详细的清理 SQL 吗？** 🚀
